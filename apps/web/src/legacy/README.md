Legacy-only frontend assets live here.

- `mocks/`: historical fixtures retained for reference while production code moves to `src/features/` and `src/data/`.

Do not add new production-facing code under `src/legacy/`.
