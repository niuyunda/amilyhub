# Runbook: API Discovery from Existing SaaS

1. Open target module page.
2. Trigger search/filter/pagination/create/edit flows.
3. Capture network endpoints (XHR/fetch), including query/body schemas.
4. Add to `docs/02-discovery/api-catalog-master.md`.
5. Store raw traces under `docs/api-catalog/raw/` if exported.
6. Update `docs/04-operations/work-log.md` + `docs/handover/current-status.md`.
